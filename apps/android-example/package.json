{
  "name": "enriched-markdown-android-example",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "./gradlew installDebug && adb shell am start -n swmansion.enriched.markdown.android.example/swmansion.enriched.markdown.android.example.AndroidExampleMainActivity",
    "build:android-native": "./gradlew assembleDebug --no-daemon --console=plain",
    "build:release": "./gradlew assembleRelease",
    "clean": "./gradlew clean"
  }
}
